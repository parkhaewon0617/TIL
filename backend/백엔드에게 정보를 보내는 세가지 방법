# 백엔드에게 정보를 보내는 방법 세가지

1. path parameter: 어떤 리소스를 식별하고 싶으면 path

2. query parameter: 정렬이나 필터링을 한다면 query 

3. request body

코드오류.
코드를 작동시키려는데 터미널에 자꾸 이렇게 뜬다. 
➜  Wordle git:(main) ✗ python -u "/Users/haewonpark/CS

간단히 말씀드리면 깃 이라는 것은 작업 중간중간 상태를 저장하고 관리하는 역할을 해줍니다. wordle git: (main) x라고 뜨는 부분은 현재 터미널 명령어가 작용하는 위치를 나타내는데,
wordle이라는 것은 작업 파일 이름을 뜻하고, git: (main)은 해당 작업의 main 브랜치(저장소)에 작업중이라는 뜻입니다 x처럼 보이는 저건 그냥 '여기서부터 명령어 입력해라' 정도의 기호로 봐주시면 될 거같아요. 에러메시지가 아리나 정상 상태이니 그대로 진행해주시면 됩니다!